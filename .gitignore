node_modules/
package-lock.json
docs/
profile/preview.html

# Build outputs (regenerated by Action — committed via explicit `git add metrics/*.svg`
# in .github/workflows/build-profile.yml which bypasses this ignore).
# Local stub files in metrics/ are kept private; only Action-generated SVGs ship.
metrics/*.svg
github-metrics.svg
# Real screenshots (manually captured + committed; Action overwrites if plugin_screenshot enabled)
!metrics/screenshot-*.png
